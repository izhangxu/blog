<%- include header.ejs %>
<%- include head-info %>
<%- include right %>
<div class="m-left">
	<%- include alert %>
	<form method="post" action="/edit/<%= post.name %>/<%= post.time.day %>/<%= post.title %>">
		<h4>标题：</h4>
		<input type="text" name="title" class="form-control"  value="<%= post.title %>" disabled="disabled" />
		<h4>标签：</h4>
		<input type="text" name="tag" class="form-control"  value="<%= post.tags.join(',') %>" disabled="disabled" />
		<h4>标签：</h4>
		<input type="text" name="type" class="form-control"  value="<%= post.type %>" disabled="disabled" />
		<h4>正文：</h4>
		<textarea name="post"><%= post.post %></textarea><br/>
		<input type="submit" class="btn btn-default" value="保存修改" />
	</form>
<%- include footer %>